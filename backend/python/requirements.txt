fastapi
uvicorn
pydantic

# Database
sqlalchemy
psycopg2-binary
alembic
python-dotenv

# Use CPU-only PyTorch wheels to keep image size below Railway's 4GB limit.
# The extra index points to the official CPU wheel repo.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu

# Demucs for source separation (compatible with torch 2.2 CPU).
demucs

python-multipart
python-jose[cryptography]
httpx
